import React from 'react';

const Footer = () => {
    return (
        <footer className="footer" style={{ marginTop: '5rem' }}>
            Footer
        </footer>
    );
};

export default Footer;
